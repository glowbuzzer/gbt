export enum AngularUnits {
    DEG = "deg",
    RAD = "rad"
}

export enum LinearUnits {
    MM = "mm",
    IN = "in",
    M = "m",
    CM = "cm"
}

export enum MatrixOrder {
    ROW_MAJOR = "row-major",
    COLUMN_MAJOR = "column-major"
}

export enum DhType {
    CLASSIC = "DH format classic",
    MODIFIED = "DH format modified"
}

export enum ExtentValues {
    MM200 = "200mm",
    MM500 = "500mm",
    M2 = "2m"
}
